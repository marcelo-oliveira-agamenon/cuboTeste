import React from "react";
import { ContainerHeader, TextHeader, ContainerText } from "./styles";

const Header = () => (
  <ContainerHeader>
    <ContainerText>
      <TextHeader>Movies</TextHeader>
    </ContainerText>
  </ContainerHeader>
);

export default Header;
